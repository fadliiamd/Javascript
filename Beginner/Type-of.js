function checktypedata() {
    var random = {
        nama: 'abdul',
    }

    console.log(typeof(random))
}

checktypedata()

// typeof merupakan fungsi untuk mengecek type data sebuah variabel
// contoh pada variabel random d atas merupakan sebuah type data object
// maka ketika dilakukan console.log akan tampil type data object