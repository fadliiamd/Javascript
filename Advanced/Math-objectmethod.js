    // Math.abs(x)
    // Math.acos(x)
    // Math.acosh(x)
    // Math.asin(x)
    // Math.asinh(x)
    // Math.atan(x)
    // Math.atanh(x)
    // Math.atan2(y, x)
    // Math.cbrt(x)
    // Math.ceil(x)
    // Math.clz32(x)
    // Math.cos(x)
    // Math.cosh(x)
    // Math.exp(x)
    // Math.expm1(x)
    // Math.floor(x)
    // Math.fround(x)
    // Math.hypot([x[, y[, ...]]])
    // Math.imul(x, y)
    // Math.log(x)
    // Math.logip(x)
    // Math.log10(x)
    // Math.log2(x)
    // Math.max([x[, y[, ...]]])
    // Math.min([x[, y[, ...]]])
    // Math.pow(x, y)
    // Math.random(x)
    // Math.round(x)
    // Math.sign(x)
    // Math.sin(x)
    // Math.sinh(x)
    // Math.sqrt(x)
    // Math.tan(x)
    // Math.tanh(x)
    // Math.trunc(x)